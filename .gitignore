# Vendor deliverable: 3.0 GB of Capella SLCs. Redistributing it is neither ours to do
# nor useful -- src/stac.py and src/p1_prep.py rebuild everything downstream of it.
anrf-aise-hack-2-0-round-3-sar-crop-yield-forecasting/

# Derived rasters, 351 MB. Regenerated by p1_prep.py from the vendor data.
results/cache/

# Privacy: land records carry owner names. Only the crop column was ever extracted and
# only aggregate accuracy is ever reported. These never enter the repo.
ground_truth_vf12.csv
.kdss_token


*.key
*.token
gee_oauth.json

__pycache__/
*.pyc

# Markdown working notes stay out of the submitted repository: the writeup, the deck
# outline, the kickoff brief and the internal research log are drafting material, and
# the check-in is submitted as MIDNIGHT_CHECKIN.pdf / .docx instead. Those two are
# deliberately NOT ignored -- they are the documentation the jury receives.
*.md
*.html
